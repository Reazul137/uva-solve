#include <bits/stdc++.h>

using namespace std;
vector<int>v;
queue<int>q;
int main()
{
    int row,col,b_row
    while(scanf("%d%d",&row,&col) != EOF){
        scanf("%d",&b_row);
        for(int i = 0; i < b_row; i++){

        }
    }
    return 0;
}
