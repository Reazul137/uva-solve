#include <stdio.h>

int main(){
    int T,a,i,c,e,f,g,h;
    char b,d;
    scanf("%d",&T);
    for(i=1;i<=T;i++){
        scanf("%d %c %d %c %d",&a,&b,&c,&d,&e);
        scanf("%d%c%d%c%d",&f,&b,&g,&d,&h);
        if( ( e > h || e == h){
            printf("Invalid birthday\n");
        }
    }
    return 0;
}
